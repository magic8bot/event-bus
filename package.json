{
  "name": "@magic8bot/event-bus",
  "version": "1.0.0",
  "main": "./dist/event-bus.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "npm run clean && tsc",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^23.3.1",
    "jest": "^23.4.2",
    "rimraf": "^2.6.2",
    "ts-jest": "^23.0.1",
    "ts-node": "^7.0.0",
    "typescript": "^3.0.1"
  }
}
